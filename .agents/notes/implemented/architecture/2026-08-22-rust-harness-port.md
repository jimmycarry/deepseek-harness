# Agent Note: Rust harness port keeps TypeScript as the behavior source

Status: implemented

English | [中文](2026-08-22-rust-harness-port.zh.md)

## Problem

A second implementation language cannot invent a second product. If the Rust tree grew its own loop, event names, or session-log rules, every later change would have two answers and no way to say which one users get. The TypeScript tree under `packages/` already owns those names: there is no privileged kernel, and a running `dsh` is the Cordis plugin tree assembled from profile, bundle, and patch layers.

## Decision

`rust/` is a Cargo workspace that ports the same plugin tree, capability seams, and session-log contract. Crate names are `dsh-<pkg>`; directories mirror `packages/<group>/<pkg>/`. TypeScript remains the behavior source of truth: a Rust change that would diverge from a TypeScript name or log event updates the TypeScript owner first, or it does not ship.

The workspace targets Rust 1.83 and edition 2021. Dependencies that pull edition 2024 or `hashbrown` 0.17 are rejected at the lockfile. Loader YAML is a hand-written subset rather than `serde_yaml` and parses the Include dialect: `- insert:` lists, id-targeted whole-field replacement, `inject`, `|` / `>-` blocks, flow sequences, and `!!js` stored as `{ "__jsExpr" }`. HTTPS provider calls go through `curl` rather than `reqwest`; the host HTTP listener is a hand-written HTTP/1.1 server rather than `axum`. `Branded<B>` implements `Clone` and `Debug` by hand so the brand token does not have to.

A capability seam is complete: Service Definition, Service Provider, and Consumer. Tool Consumers depend only on the Definition crate. Deployment-varying values are `Config` fields supplied at construction; `run` does not hide a default. Model-visible content is logged; compaction advances the surface with `surfaceOp: replace` and does not delete history. Session logs stay on `SESSION_FORMAT_VERSION` `0`; the SQLite backend uses monotonic `SCHEMA_VERSION` `2` and stores the session header alongside the event rows. Unknown required-on-read event types refuse resume unless the envelope carries `ignorable: true`.

Composition identity is the row `id` plus the TypeScript plugin `name` (`@deepseek-ai/dsh-*` or `@deepseek-ai/cordis-plugin-*`), not the Rust crate name. `dsh --dump-config` and `compose_profile` share one flattened `apply_entry_patches` call over the TypeScript `dsh-base` then `dsh-headless` patch files; `!!js` prints verbatim and is not evaluated. A missing patch target or a name mismatch fails loud. The default driver lives in `dsh-agent-loop` and stays a plugin. `max-tokens` is sticky for the open turn. `agent/turn-stopping` may `steer` another step. The first `cancel` reason wins. Tool bodies overlap up to `ToolRuntimeConfig.max_parallel`; `tools/post-execute` commits in model order. A task run composes that same tree, registers every plugin name, and mounts it. `!!js` on `disabled` and `config` is evaluated at mount (`process.env.*`, `process.platform`, `process.cwd()`, `dshHomePath`, `ctx.<service>.<field>`). Spine rows, default-model, persistence, sandbox-policy (`ctx.sandboxPolicy` resolves last `sandbox/mode` else the configured default; omitted `mode` is `read-only`; `sandbox:policy` order 110 renders from the current session; bash and `write`/`edit` honor that per-call policy; when the live `ctx.fs` or `ctx.shell` confines, those tools advertise `sandbox_permissions`/`justification` and resolve a strictly-wider retry through `ctx.approval` before executing; `FS_SANDBOX_DENIED` maps to the shared `[sandbox: …]` marker, with the operation hint on `write`/`edit` and the marker only on `str_replace_editor`), user-approval (`ctx.approval`; `never` rejects without prompting, `ask` fails closed as `unavailable` without an answerer; the `approval/asked` + `approval/decided` pair must sit in an open turn; `approval:policy` order 115), permission-presets (`ctx.permissionPresets` plus `/permission`; headless `pin_initial` writes the three knobs on the root session only), credentials-local (inherited env, then `$DSH_HOME/.credentials.yaml`, then `<cwd>/.env`, then `$DSH_HOME/.env`; a Unix document other users can read is refused; `llm-deepseek` and DeepSeek search resolve through `ctx.credentials` and fail with the TypeScript `MISSING_CREDENTIAL` sentence), settings, fs-sandbox, bash-sandbox, tool-fs-search, tool-str-replace-editor, goal / goal-round-driver / command-goal / tool-goal, subagent spawn/fork, tool-subagent, tool-subagent-control (plus `/list-agents`), tool-subagent-report, workflow-worker-thread / tool-workflow, tool-ralph (the TypeScript fixed fresh-agent loop in Rust over `ctx.subagents` `spawn`, which must advertise structured output and `inheritsParentContext: false`; it does not evaluate `RALPH_SCRIPT` or start a workflow worker), repeat-tool-reminder, web / web-search-deepseek / tool-web, attachment-local, session-query-sqlite, spill-local / spill-policy, tool-todo, tool-call-timeout-policy, tool-result-pruner, plan-mode, user-questions, skill / skill-filesystem / tool-skill, token-meter, compaction-basic / command-compact, session-projection, session-checkpoint-policy, llm-retry, agent-instructions, jobs-local / tool-jobs, command-feedback (`/feedback`; `recordInput: false`; log-only `command/run` then `feedback/record` then `command/done`; the acknowledgement cites the harness-home `.anonymous-user-id` minted by `dsh-anonymous-user-id` — a bare UUID line, memoized per path, wx-created, and still returned from memory when the write fails — plus the optional `ctx.sessionTelemetry` sharing sentence), fs-observation-policy (no service; `fs/write-intent` / `fs/edit-intent` occupy the single decision slot and do not call `next()`; `fs/observed` is keyed by `agent.session.id`; the local backend honors `createIfAbsent` / `replaceIfVersion` and tools append the TypeScript recovery sentences), session-telemetry-otel (default DISABLED disclosure; FULL / FEEDBACK_ONLY export OTLP/HTTP JSON logs; a 429/502/503/504 or transport error retries inside processor.exportTimeoutMillis, each HTTP attempt capped by exporter.timeoutMillis, honoring Retry-After delay-seconds and HTTP-date on HTTP and HTTPS, passing exporter.keepAlive (default true; HTTP/1.1 socket reuse; HTTPS one-shot curl cannot reuse a TLS socket), or exponential backoff, then drops the batch; a full queue drops the newest record; the seam flush hint is unimplemented and the crate emits no SDK metrics), settings-file (`$DSH_HOME/settings.yaml`; `watch` defaults to true; later `section` / `document` reads reload after `debounceMs`; a live parse failure keeps the last good document; `update` / `replace` persist one registered namespace as a comment-preserving YAML leaf-level diff, or pretty JSON for `.json`; `llm-deepseek` registers that namespace and re-reads `defaultContextWindow` plus the per-model catalog on each `resolve_model` (default 1,000,000; V4 Flash / Pro / Flash-Vision-Exp; an invalid live snapshot keeps the last good catalog), fs-sandbox (read-only / workspace-write / danger-full-access write fencing), pwsh-sandbox / tool-pwsh (the `!!js` `process.platform !== 'win32'` disable still skips those rows on POSIX), and the headless startup/runner apply for real; remaining names mount as no-ops. `ctx.sandbox.confine` wraps argv through bwrap then landlock-run, or fails closed with the TypeScript `SANDBOX_UNAVAILABLE` sentence; each wrap carries `runnerFailureRules` (bwrap: `bwrap: `; landlock: exit 125 plus a `landlock-run: ` fatal line after excluding the exact `landlock-run: partial enforcement (older Landlock ABI)` notice; seatbelt: `sandbox-exec: `; windows-acl: exit 127 plus `windows-acl-run: `; denial signatures `access is denied` / `access to the path` / `permission denied`). `dsh-sandbox-windows-acl` derives the TypeScript `workspaceWriteSid` / `tempWriteSid` and rejects overlapping temp/workspace paths; Windows confinement prefixes the TypeScript Node runner (`node` plus `lib/runner.js`, or `--import tsx/esm` plus `src/runner.ts`) with the stable `--workspace` / `--temp` / `--mode` argv contract; this crate does not call CreateRestrictedToken; Wine and Linux cannot prove NTFS/DACL. `danger-full-access` runs bash unconfined. Confined wraps stamp `denialSignatures` (`bwrap`: `read-only file system`; landlock: `permission denied`) and classify `sandbox.denied` from stderr; runner-failure evidence is classified first and is not a denial — a foreground match throws `SANDBOX_UNAVAILABLE` with `Runner failure: {detail}`, and a settled background process stamps `runnerFailed: true` with `denied: false`; a missing or unexecutable confine argv[0] with a usable cwd follows the same split. Foreground `run` returns a structured result so a nonzero exit is reported, not an infrastructure error, and the bash tool renders the shared denial marker plus the command escalation hint. Background bash starts through `ctx.shell.start` under the same confine path and maps the handle into `ctx.jobs`; `job_output` appends the denial marker after settlement, or the runner-failure notice when `runnerFailed` is set. The bash and pwsh tools share an `output` oneOf (`kind: background` with `jobId`, or `kind: foreground` projecting the run including optional `sandbox.runnerFailed`), then render the same model-visible text (`started background job {id}` and the exit/timeout/sandbox markers); `presentCall` / `presentResult` are a foreground terminal card or a background/error generic `console` fence, and malformed args return no view. `parseExitStatus` lives on `dsh-shell`. `dsh-pwsh-local` / `dsh-pwsh-sandbox` / `dsh-tool-pwsh` mount for real (`pwsh -NoLogo -NoProfile -NonInteractive -Command`, confining that argv rather than `bash -c`). The bash schema requires `command` and `description` and advertises `timeoutMs` and `workdir`. `ToolCall.call_id` is passed into `ctx.approval`. `ctx.shellEnv` injects trusted `DSH_HOME` / `DSH_SHELL=1` / `DSH_SESSION_ID` into model bash, plus `DSH_SESSION_JSONL` when the JSONL backend locates one; plugins register extra `DSH_*` keys. `glob` and `grep` spawn `rg` through `ctx.subprocess`. `str_replace_editor` uses `ctx.fs` and requires an absolute path. `ctx.goals` is event-sourced: mutations append `goal/change`, and `goal-round-driver` queues a `<goal_round>` followup on `goal/changed` for an armed active goal. `create_goal` / `get_goal` / `update_goal` are the model tools; `/goal` is not model-visible. `tools/post-execute` carries `name`, `args`, `agentId`, `content`, and `additionalContexts`; denied calls still run that waterfall. `repeat-tool-reminder` prepends a plugin notice at the configured thresholds (`[3, 5, 8]` in base) and resets the chain when `agent/pre-step` sees a human `source.kind === "user"` message. `web_search` goes through `ctx.web` with provider id `deepseek-official`; live search POSTs `${baseURL}/messages` via `curl`, and a `replay` config serves keyless snapshots. Base `tool-web` keeps `fetch: false`. `subagent` delegates through `ctx.subagents`: a foreground call collects the child's result, and the default background path starts a continuable child (`subagent/descriptor` version 2, `origin: "subagent"`, parent `delegationDepth` + 1; requiring `ctx.sessionPersistence` with the TypeScript persistence sentence before any child session is published; a caller-reserved child id is rejected when the live registries or configured persistence already own it, with `subagent "<id>" already exists`) and answers `started subagent <id>`. Both in-process starts capture the parent session's explicit sandbox override (`sandboxPolicy.overrideOf`) before any await and, when `ctx.approval` is composed, pin the child's approval to `never` regardless of the parent policy; those values are appended as `source: "delegation"` `sandbox/mode` / `approval/policy` events after any fork seed and before `agents.create`, so a cold resume replays the child's log instead of recapturing. `subagent:delegation` (order 120) is a Dynamic runtime-context sentence emitted only when `origin` is `"subagent"` or `delegationDepth` is greater than 0. `send_message` queues a later turn on that durable child by id — resuming a resident child from the catalog, or cold-loading its persisted Session when the catalog misses (requiring `ctx.sessionPersistence`; folding the descriptor after `seedLength`) — with a `coordinator` relay source; `interrupt_agent` cancels a live descendant's current turn; `list_agents` projects continuable children as `<id> [running|idle|ready] — <label>` (with ` parent=<id> depth=<n>` under `scope: descendants`) from the `subagent` unit on `ctx.sessionProjections`, merging persisted `origin: "subagent"` headers when persistence is mounted, reporting unreadables as `<id> [diagnostic: corrupt|unavailable]` instead of dropping them (a live child without an identity yet is omitted; a cold fold with no identity or a lifecycle mismatch is `corrupt`; a failed cold inspect is `unavailable`; listing inspects without `commitRepair`), and fails with `listing subagents requires the sessionProjections registry (load @deepseek-ai/dsh-session-projection)` when that registry is missing; and the child-scoped `report`, visible only to resident continuable children through `Tool::enabled_for`, relays framed content to the direct parent (`next-step` steers, `quiet` injects) with a `subagent-report` relay source. When a quiescent childless activation settles, the manager disposes it and wakes the parent with a `subagent-settled` notice (bounded 120-character summary) carrying the stop-reason sentence and the child's closing message; the headless runner drives `run_pending` until the whole tree is quiet. A resumed child continues turn numbering from its session log instead of restarting at 1. `subagent_fork` stays one-shot; one-shot `run_in_background: true` starts a `subagent-N` job when `ctx.jobs` is mounted, otherwise it fails with the background-jobs-unavailable sentence. The loop emits `agent/inbox/claimed` so a human claim resets the completion-wake budget. `workflow` evaluates a top-level `return <json>` body and writes `tool-workflow/run-start` / `run-end`; it does not start a JavaScript worker. `ctx.attachments` stores admitted source images at `$DSH_HOME/attachments/v1/objects/<aa>/<sha256>` after magic-byte and header-dimension checks; it does not raster-decode or downscale. `ctx.sessionQuery` serves exact reads, titles, and lineage from live and persisted logs; headless `openAt: never` fails `searchSessions` / `searchEvents` with `SESSION_QUERY_SEARCH_DISABLED` and never opens SQLite. `session-persistence-sqlite` writes monotonic `SCHEMA_VERSION` `2` with the JSON session header on the session row plus one JSON event row per seq, and refuses any other `user_version`. `ctx.sessionPersistence.inspect` returns a validated header and event log with in-memory crash-repair closers and does not rewrite storage or publish a Session; `load` still reconstructs a Session for resume. `list_snapshots` / `read_stored_revision` expose a backend-owned revision token (JSONL: TypeScript `dev:ino:size:mtimeNs:ctimeNs` file-stat identity; SQLite: `sqlite:{path}:events:{count}:header:{sha256}` until the TypeScript incarnation counter lands) while `inspect` shares in-flight loads, retries when that token changes, and keeps a ready LRU of `preparedSessionCacheSize` (default 5); `list` / `list_snapshots` still read the backend directly. `spill-policy` listens on `tools/post-execute` (which carries `content`) and, when a plain-text result exceeds `maxInlineBytes`, saves the full text through `ctx.spillStore` and replaces the model-facing result with a head/tail preview plus locator; `read` is skipped and a save failure keeps the inline result. dump-config never mounts and never evaluates. `apply_world` remains for crate tests that exercise the spine without the profile tree. A slash command is dispatched by `ctx.commands.execute` on the headless runner and does not enter the model. Session JSONL leads with the TypeScript header line (`type: "session"`, `version`, `id`, `createdAt`, `cwd`, `delegationDepth`); each event — typed and Extension alike — serializes as `{type, seq, time, data}` and carries `sourceEventSeqs`, `surfaceOp`, and `ignorable` only when present. Messages carry `role` and a UUID `id`; `assistant/message` cites its `assistant/chunk` seqs and `tool/result` cites its `tool/call` seq through `sourceEventSeqs`. `StreamChunk` uses the TypeScript `type` tags (`block-start`, `text-delta`, `tool-call-delta`, `block-end`, `usage`, `finish`) and `FinishReason` is `{kind}`. `TokenUsage` uses `inputTokens` / `outputTokens`. User messages carry `source.kind`. The loop records `agent/inbox/spliced`, `request/header`, `request/context`, and the system-prompt runtime-context snapshot; `request/header` follows the TypeScript `canonicalHeader()` omission rules (`config`, then `adapterDefaults` / `system` / `tools` only when populated). `ctx.sessionTitle` writes the deterministic fallback `session/title`, and the first-prompt provider logs the exact model-visible `session/title-llm-request` before an auxiliary `purpose: "session-title"` call; an auxiliary failure keeps the standing title. Headless `pin_initial` writes `permission/preset`, `sandbox/mode`, and `approval/policy` from the composition default before the first followup. `todo_write` requires the boolean `allowParallelInProgress` Config, validates content / status / duplicates / the single-in-progress rule, appends the log-only `todo/write` snapshot, and renders `Updated todo list: N pending, N in progress, N completed.`. `tool-call-timeout-policy` has no Config: it enforces a tool's registration-declared `timeout_ms` deadline and replaces the body result with `Error: tool call timed out after {timeoutMs}ms` (`TOOL_TIMEOUT`); tools without a declared deadline are untouched. `ctx.toolResultPruner` runs inside compaction, not on the execute pipeline: each over-`thresholdChars` surface `tool/result` gets a log-only `compaction/prune` record immediately followed by the `tool/result` replacement (`surfaceOp: replace`, `sourceEventSeqs: [seq]`) with the fixed middle-pruned marker. `plan-mode` requires a non-empty `section` Config rendered as prompt section `plan:policy` (order 50) only while active, commits transitions as log-only `plan/mode`, and registers `exit_plan_mode` plus `/plan`; the review rides `ctx.userQuestions`, and headless mounts that service without a provider so `exit_plan_mode` fails with the exact `no user-questions provider is registered` sentence and stays in plan mode. `ctx.skills` registers `name/SKILL.md` bundles and flat `*.md` skills discovered by `skill-filesystem` under project `.dsh/skills`, `.agents/skills`, `customSkillDirs`, and `$DSH_HOME/skills` (frontmatter `name` / `description`, `disable-model-invocation`); `tool-skill` publishes the catalog as a `user/message` with `source.kind: "skill-catalog"` on `agent/pre-step` and republishes with `update: true` when the name/description digest changes; loads render in the `<skill_content>` frame. `token-meter` provides `ctx.tokenMeter` (`charsPerToken`, default 4). `compaction-basic` uses the TypeScript ratio policy (`thresholdRatio` default 0.8, `retainRatio` default 0.16; `retainRatio` and `retainTokens` are mutually exclusive; `modelPolicies` applies exact provider/model overrides; pressure asks `ctx.llm.resolve_model_info` for the routed adapter `contextWindow` and fails with the TypeScript no-capacity or retain-above-threshold sentence when that metadata is missing or unusable; a retain cut walks head-ward until `toolPairingBalancedBefore` so an assistant tool-call/result pair is never split; pressure retries until `compactionRetries` is exhausted and then fails with the TypeScript still-above-threshold sentence; canonical `CONTEXT_WINDOW_EXCEEDED` on `agent/request-error` authorizes `{kind:retry}` only after `surface.replaceGeneration` advances, counts attempts per session up to `maxOverflowRetries` (default 1; `0` disables recovery only), and resets that count after a later `assistant/message` or `agent/status` idle; `/compact` via `commands.execute` targets the invocation session) and summarizes through `ctx.llm.stream()` with `purpose: "compaction"` (the TypeScript compaction instruction as the final user message; the checkpoint wraps that output in the TypeScript preamble plus `<compacted-summary>` as separate text blocks; a framed checkpoint that is not smaller than the shadowed span, or a failed call, writes `compaction/end.error` and `/compact` answers the TypeScript summary-failure sentence; a cancelled agent, a surface that changed during summarization, a failed append, or a durability checkpoint failure map to the TypeScript `cancelled` / `changed` / `commit` / `persistence` sentences) and writes the TypeScript event vocabulary: `compaction/start` / `compaction/summary` / `compaction/end` share a `compactionId` (plus `sourceCommandId` for `/compact`); `compaction/summary` carries `ContentBlock[]` `summary` plus `provider` / `model` / `rawOutput` / `llmStreamCall: true`; and the checkpoint `user/message` replaces the shadowed range while citing the start seq, summary seq, and shadowed seqs; `/compact` answers `Compacted N history items (~T tokens).` and cites the `compaction/summary` seq on `command/done.sourceEventSeq`. `agent-instructions` requires `maxBytes` (base 65536), discovers `AGENTS.md`/`CLAUDE.md` under the project root plus `$DSH_HOME/AGENTS.md`, and publishes the first baseline as a `user/message` with `source.kind: "agent-instructions"` wrapped in `<system-reminder>`; a later step whose discovery identity changed publishes a replacement baseline, and a file that appeared, changed, or disappeared under the same identity publishes a non-baseline update. `session-checkpoint-policy` flushes through `ctx.sessionPersistence` before `agent/pre-step`, `llm/stream`, and top-level `tools/execute`; a missing persistence backend is a successful no-op, and JSONL dispose drains every live session, and a flush failure fails closed. `llm-retry` has no Config (`retryPolicy` belongs on each provider); the loop copies the adapter route's `retryPolicy` onto `agent/request-error`; a matching listener writes `llm/retry` then `llm/retry-started` and returns `{kind:retry}`; a valid positive `providerRetryAfterMs` at or below `maxDelayMs` replaces local backoff, while an over-cap instruction makes `normal` delegate and `always` use local backoff. `llm-deepseek` classifies non-2xx responses with the TypeScript status and provider-detail codes and retains HTTP status, a valid positive `Retry-After` delay-seconds or HTTP-date, and `x-request-id` / `x-deepseek-request-id`.

Two automation stdio servers ride the same base tree as shipped profiles composed from `dsh-bundle-acp` and `dsh-bundle-jsonrpc` patch layers (`dsh --profile acp` / `dsh --profile jsonrpc`; both keep hmr off and insert their server row over `dsh-base`). `dsh-acp` answers `initialize` with the fixed handshake (`protocolVersion` 1, `deepseek-harness-acp` 0.0.1, image/audio/embeddedContext all false, empty `authMethods`), `authenticate` with `{}`, validates `session/new` (absolute `cwd`; `mcpServers` and `additionalDirectories` rejected) with the TypeScript `Invalid params: <detail>` wording, admits text-only `session/prompt` blocks with the exact per-type rejection sentences, emits one `session/update` `agent_message_chunk` per committed assistant text block, and answers the TypeScript settlement stop-reason mapping (`interrupted` → `cancelled`; `completed` / `max-tokens` / `aborted` / `blocked` → `end_turn`; an `error` ending rejects with `Internal error: turn failed: <message>`); `session/cancel` is an id-less notification that marks the addressed session and calls `agent.cancel({ kind: "user" })` so an in-flight prompt can settle as `cancelled`; a second `session/prompt` for that session fails with `a prompt is already in flight for this session`; stdin is read on a dedicated thread while the prompt task runs; unknown methods answer `"Method not found": <method>` (`-32601`). `dsh-sdk-server` keeps the wire-stable `deepseek-harness-sdk-runtime` identity, validates `initialize` (`maxTokens` positive; a missing adapter fails `no adapter registered for provider "<provider>"`), lazily creates one agent per `session/prompt` `sessionId` under the initialize `cwd`, revalidates the record against the live registry before delivery, streams `session.event` for every logged event of the turn bracketed by `session.status` `running` / `idle` (the enqueue splice precedes `running`, matching the TypeScript notification order) and inserts `subagent.started` / `subagent.finished` collected during the driven turn before `idle`, answers the `messageId` receipt, and `shutdown` disposes server-owned agents while the context keeps running. The TypeScript servers enqueue and stream while the agent runs in the background. The Rust ACP and JSON-RPC servers match that timing on one ordered stdio stream: store-backed `Session::append` publishes `session/event` after commit; ACP writes each committed assistant text block as `session/update` `agent_message_chunk` while `session/prompt` waits for idle; JSON-RPC returns `{ messageId }` as soon as `followup` succeeds and streams `session.event` / `session.status` / `subagent.*` from the live bus. `agent/status` carries lowercase `running`/`idle` (maintenance maps to idle). ACP still reads later frames (including `session/cancel`) while that prompt task runs. `dsh-sdk-client` skips id-less frames while awaiting a response, collects them in wire order, and projects `finalResponse` from the last committed `assistant/message`; the `dsh-webserver` `/rpc` route serves the same protocol request/response-only.

## Alternatives considered

- **Port the loop as one `async fn` and pluginize later** — the TypeScript rule is the opposite: new behavior lands on documented extension points. A monolith would drop `agent/pre-step`, `agent/request`, `agent/request-error`, and `agent/turn-stopping` as the place plugins actually run.
- **Rewrite the Web UI in Rust** — the host only has to speak the existing client protocol. A second UI is a product fork, not a port.
- **Fold compaction into the loop** — TypeScript compaction is a Consumer of `agent/pre-step`, `agent/request-error`, and idle maintenance. Putting it in `step` would make every later engine a loop change.
- **Treat the Python SDK as a second loop** — both SDKs project the TypeScript loop. A Rust loop that those SDKs also reimplement would be a third driver.
- **`serde_yaml` / `reqwest` / `axum`** — each pulls an edition-2024 or `hashbrown` 0.17 graph that Rust 1.83 cannot build. The hand-written YAML subset, `curl` transport, and HTTP/1.1 listener keep the workspace on the declared toolchain.
- **Compatibility shims for older on-disk formats** — the pre-release stance rejects old backends. A shim would hide a format mistake until the first tagged release.

## Consequences

The repository carries two language trees until a tagged release. `cargo test --workspace` under `rust/` is the Rust evidence; keyless spine snapshots live in `rust/apps/cli/tests/headless_snapshot.rs` and cover a text turn, a `bash` turn, a background `bash` job collected with `job_output`, a `write` / `edit` turn, a `glob` turn, a `str_replace_editor` create that rereads the file from the host, `create_goal` / `get_goal` plus an admitted `<goal_round>`, `web_search` against a replay provider, `workflow` `return <json>`, a foreground `subagent` child, a `ralph` turn whose parent result is the worker-reported completion text, a third identical `bash` call that injects the gentle repeat-tool-reminder notice, and a 60 000-byte `bash` result replaced by spill-policy with a host-rereadable locator. Profile-path snapshots in `rust/tests/snapshots` compose the headless tree and compare type sequence plus key payloads to the TypeScript headless-profile vocabulary, including those same orchestration and spill turns, the `session/title-llm-request` payload, a workspace `AGENTS.md` baseline whose `user/message` carries `source.kind: "agent-instructions"` and the `<system-reminder>` frame, a later `write` of that file that publishes the TypeScript `Updated instructions from:` update, the `sourceEventSeqs` citations on `assistant/message` and `tool/result`, a `todo_write` turn that lands the `todo/write` snapshot and the counted result line, a `skill` turn that publishes the `skill-catalog` message and loads the `<skill_content>` frame, a later write of a new `SKILL.md` that republishes the catalog with `update: true`, an `exit_plan_mode` call outside plan mode that fails with the exact guard sentence, and the continuable subagent paths: a background delegation whose settlement notice carries the child's closing message into the parent's second turn, a child `report` that precedes the settlement notice in the parent log, a `list_agents` row plus the exact unknown-id `send_message` failure, and a post-settlement `send_message` that resumes the child with a `coordinator` follow-up continuing turn numbering and keeping a single `source: "delegation"` policy seed, plus a catalog-evicted `send_message` / `list_agents` after the child is saved and removed from the live store, plus a descriptor-less persisted child listed as `{id} [diagnostic: corrupt]`, plus a background `bash` job whose `job_output wait: true` result ends with `[status: completed, exit code: 0]`, and a mounted `/feedback` `execute` that writes `command/run` / `feedback/record` / `command/done` and discloses `Session sharing is disabled.` when telemetry is DISABLED, a session-telemetry-otel overlay that posts OTLP/HTTP JSON to a mock collector in FULL (ledger plus ops, redacting a fixture key while the canonical JSONL keeps it), FEEDBACK_ONLY (prefix through `feedback/record`, excluding a later private suffix), and DISABLED (empty captures plus the local disclosure warning), a headless-runner slash `/feedback` that never opens a model turn, plus a mounted `write` of an unobserved existing file that fails with `FS_NOT_OBSERVED` and then succeeds after `read` and `edit`, a mounted settings-file reread of `$DSH_HOME/settings.yaml` after an external edit, a comment-preserving `update` of the `llm-deepseek` section, a `bash` turn whose tool result includes `DSH_HOME` / `DSH_SHELL=1` / `DSH_SESSION_ID`, and a mounted `/compact` `execute` that writes `command/run` / `compaction/start` / `compaction/summary` / `compaction/end` / `command/done`, cites the summary seq on `command/done.sourceEventSeq`, records `provider` / `model` / `rawOutput` / `llmStreamCall` plus the `ContentBlock[]` summary on `compaction/summary`, applies a `modelPolicies` `maxTokens` override, lands a multi-block checkpoint containing the replayed summarizer text, and a second `/compact` that keeps a closed tool-call/result pair in the retained tail, plus a provider `CONTEXT_WINDOW_EXCEEDED` recovery that writes the compaction bracket and then answers the retried turn, a provider `RATE_LIMIT` that writes `llm/retry` then `llm/retry-started` and answers `RETRY_OK`, a mounted `/permission read-only` that then denies `write` with the TypeScript `[sandbox: file access denied under read-only mode]` marker plus the operation escalation hint, a same-turn `sandbox_permissions` retry that writes after an auto-allow, and a pressure pass that reads adapter capacity from a replay `providers` catalog rather than a logged `contextWindow`. A three-end fixture (`rust/tests/snapshots/tests/three_way_profile.rs`) drives one replayed task through the headless, ACP, and JSON-RPC profiles and pins them to a single post-knob event-type stream and one final text, with the ACP wire byte-compared to the TypeScript fixture vocabulary after session-id normalization; built-bin smokes (`rust/apps/cli/tests/stdio_profiles.rs`) run `dsh --profile acp` and `dsh --profile jsonrpc` over real stdio. `--dump-config` prints the composed TypeScript row ids and plugin names; `dsh-app-boot` pins the headless id sequence and a replay turn that flushes JSONL under `$DSH_HOME/sessions`. JavaScript workflow workers, TypeScript packed SQLite schema 17, raster image normalization (with ACP image prompts), live DeepSeek search without a `replay` config, and host-native skill directory watching stay unmounted. Remaining TypeScript-to-Rust functional gaps and their port priority live in [ts-rust-functional-gaps.md](../../../../rust/docs/ts-rust-functional-gaps.md). Headless `dsh --resume <id>` loads that persisted JSONL, appends `session/end-seed` when the stored log does not already end with one, continues turn numbering, and logs `request/header` reason `resume` on the first request of the new loop instance. session-telemetry-otel exports OTLP/HTTP JSON logs in FULL and FEEDBACK_ONLY; a retryable collector failure retries inside exportTimeoutMillis, honoring Retry-After delay-seconds and HTTP-date on HTTP and HTTPS and exporter.keepAlive, then drops the batch; a full queue drops the newest record; the seam flush hint is unimplemented and the crate emits no SDK metrics; DISABLED still constructs no exporter.

A later crate that needs a default must take it as `Config` at construction. A later event type that older Rust readers must skip has to set `ignorable: true`; omitting the marker refuses resume. Changing `dsh-agent-loop` still requires updating [docs/architecture.md](../../../../docs/architecture.md).
